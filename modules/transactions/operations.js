import { addTransaction, removeTransaction, updateTransaction, getTransaction, resetTransaction} from './actions';

export default {
    addTransaction,
    removeTransaction, 
    updateTransaction,
    getTransaction,
    resetTransaction
};
